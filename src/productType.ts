export type ProductType = {
  id: string;
  name: string;
};

export type ImageType = {
  id: string[];
  image: string;
};

export type ModifiedProductType = {
  id: string;
  name: string;
  img: string;
};
